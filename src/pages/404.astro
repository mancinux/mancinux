---
import { sitePath } from "../lib/site-path";
import SiteLayout from "../layouts/SiteLayout.astro";
---

<SiteLayout title="404 — mancinux" description="this page took an async day.">
  <section class="not-found-section">
    <div class="shell">
      <p class="eyebrow">error / 404</p>
      <h1>this page is in a meeting.</h1>
      <p>it should have declined the invite.</p>
      <a href={sitePath()}>← return to the notes</a>
    </div>
  </section>
</SiteLayout>
